# Tasks: Remove Unrequired UI Texts

- [x] Remove `CardHeader` and adjust `CardContent` padding in `src/components/organisms/BookingCalendar.tsx`.
- [x] Remove the step indicator "Paso 2 de 2" from `src/components/organisms/BookingForm.tsx`.
- [x] Verify the layout looks clean and correctly spaced after removals.
- [x] (Optional) Run any linting or build tools to ensure no regressions.
